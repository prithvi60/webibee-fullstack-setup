const Page = () => {
    // const { slug } = params as { slug: string };

    return (
        <div>
            <h4 className="text-3xl font-semibold text-center my-10">blog</h4>
        </div>
    );
}

export default Page;